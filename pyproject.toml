[project]
name = "controller1"
version = "0.1.0"
description = "Research on controllers and decoding strategies for LLMs, evaluated on HumanEval/MBPP"
readme = "README.md"
requires-python = ">=3.12, <3.13"
dependencies = [
    "lm-eval",
    "transformers",
]

[tool.uv.sources]
lm-eval = { git = "https://github.com/EleutherAI/lm-evaluation-harness.git" }

[dependency-groups]
dev = [
    "pytest>=8.0",
]
