{
  "name": "controller1-analysis-observable",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:baselines": "node scripts/sync-baselines.mjs",
    "dev": "npm run sync:baselines && observable preview",
    "build": "npm run sync:baselines && observable build",
    "preview": "observable preview --build",
    "test": "vitest run",
    "test:smoke": "node scripts/smoke-baselines.mjs",
    "test:ci": "npm run sync:baselines && npm run test && npm run test:smoke && npm run build"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.3",
    "diff": "^5.2.0",
    "echarts": "^5.6.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
